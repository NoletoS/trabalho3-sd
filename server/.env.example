# PostgreSQL instalado localmente no Windows (não use Docker)
DATABASE_URL=postgresql+psycopg://media_app:troque_esta_senha@localhost:5432/media_processor

# Podem ser apenas ffmpeg/ffprobe quando a pasta bin estiver no PATH.
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe

STORAGE_ROOT=storage
MAX_UPLOAD_MB=500
